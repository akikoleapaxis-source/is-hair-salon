[build]
  command = "npm run build"
  publish = "dist"

# Admin redirect is handled by file existence, but fallback is good
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
